// Computer Program: This is the entire code that will be run when the button is clicked.
function exampleProgram() {
    // Instruction: A single task like declaring a variable.
    let num1 = 5; // This is a statement that declares a variable 'num1'.
    let num2 = 10; // Another statement that declares 'num2'.

    // Instruction: Adding two numbers and storing the result.
    let sum = num1 + num2; // This statement adds num1 and num2 and stores the result in 'sum'.

    // Syntax: Ensures that the code follows the rules. If I miss a semicolon here, it could cause an error.
    document.getElementById("output").innerText = "Sum of num1 and num2 is: " + sum;

    // Comment: This line is explaining that the result will be displayed on the webpage.
    // Comments are not executed by the program.
}
